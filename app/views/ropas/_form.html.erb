<%= form_with(model: ropa) do |form| %>
  <% if ropa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ropa.errors.count, "error") %> prohibited this ropa from being saved:</h2>

      <ul>
        <% ropa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :precio %>
    <%= form.text_field :precio %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
