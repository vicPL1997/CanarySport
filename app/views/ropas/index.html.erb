<p id="notice"><%= notice %></p>
<div style="margin-left: 2em;">
<h1>Toda nuestra ropa</h1>
<p> Bienvenido al panel de gestión de la ropa, aquí podrá crear, modificar y eliminar las prendas deportivas</p>
<table class="table table-striped table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Nombre</th>
      <th>Precio</th>
      <th>Descripcion</th>
      <th>Imagen</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @ropas.each do |ropa| %>
      <tr>
        <td><%= ropa.nombre %></td>
        <td><%= ropa.precio %></td>
        <td><%= ropa.descripcion %></td>
        <td><% if ropa.image.attached? %>
        <img src="<%= url_for(ropa.image)  %>" class="img-rounded img-responsive" alt="Product view">
      <% end %></td>
        <td class="enlaceboton"><%= link_to 'Ver', ropa %></td>
        <td class="enlaceboton"><%= link_to 'Editar', edit_ropa_path(ropa) %></td>
        <td class="enlaceboton"><%= link_to 'Borrar', ropa, method: :destroy, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<a class="enlaceboton" href="/ropas/new" role="button">Crear prenda</a>
<a class="enlaceboton" href="/panel_administracion" role="button">Página principal</a>

</div>

